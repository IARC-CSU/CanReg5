<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<project name="CanReg" default="default" basedir=".">
    <description>Builds, tests, and runs the project CanReg.</description>
    <import file="nbproject/build-impl.xml"/>
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-jar:                  called before JAR building
      -post-jar:                 called after JAR building
      -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are: 

      -init-macrodef-javac:     defines macro for javac compilation
      -init-macrodef-junit:     defines macro for junit execution
      -init-macrodef-debug:     defines macro for class debugging
      -init-macrodef-java:      defines macro for class execution
      -do-jar-with-manifest:    JAR building (if you are using a manifest)
      -do-jar-without-manifest: JAR building (if you are not using a manifest)
      run:                      execution of project 
      -javadoc-build:           Javadoc generation
      test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

        <target name="run" depends="CanReg-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that the overridden target depends on the jar target and not only on 
    the compile target as the regular run target does. Again, for a list of available 
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file. 

    -->
    <!-- Custom Target for AppInfo.java -->
    <target name="-pre-jar">
        <buildnumber file="buildnumber.properties"/>
        <propertyfile file="appinfo.properties"
		  comment="Everything can be manually updated except buildnum and builddate.">
            <entry key="program.PROGNAME" default="CanReg" />
            <entry key="program.AUTHOR" default="Morten Johannes Ervik, CIN/IARC" />
            <entry key="program.COMPANY" default="International Agency for Research on Cancer" />
            <entry key="program.COPYRIGHT" default="now" type="date" pattern="2008-yyyy" />
            <entry key="program.DESCRIPTION" default="" />
            <entry key="program.VERSION" default="4.99.0" />
            <entry key="program.BUILDNUM" value="${build.number}" />
            <entry key="program.BUILDDATE" type="date" value="now" pattern="yyyyMMddHHmmss" />
        </propertyfile>
        <copy file="appinfo.properties" todir="${build.classes.dir}"/>
        <mkdir dir="dist/lib"/>
        <copy todir="dist/lib" failonerror="false">
            <fileset dir="lib" includes="*.jar"/>
        </copy>
    </target>
    <target name="-build-jdeploy">
        <echo>Generating native distributions using jDeploy...</echo>
        <exec executable="npx" failonerror="true">
            <arg value="jdeploy"/>
            <arg value="package"/>
        </exec>
    </target>

    <target name="-package-zips" depends="-build-jdeploy">
        <echo>Building platform-specific zip distributions...</echo>
        
        <!-- 1. Translations zip -->
        <zip destfile="${dist.dir}/translations.zip">
            <zipfileset dir="src/canreg/client" includes="**/*.properties"/>
        </zip>

        <!-- 2. R-Packages zip -->
        <mkdir dir="${dist.dir}/web"/>
        <zip destfile="${dist.dir}/web/CanReg5-R-packages.zip">
            <zipfileset dir="conf/tables/r/r-packages" prefix="conf/tables/r/r-packages"/>
        </zip>

        <!-- 3. Base Zip Configuration (shared across platforms) -->
        <!-- We use a patternset to share the base files across all platform ZIPs -->
        <patternset id="base.files">
            <include name="conf/**"/>
            <exclude name="conf/tables/r/r-packages/*"/>
            <include name="demo/**"/>
            <include name="scripts/**"/>
            <include name="CanReg.sh"/>
            <include name="changelog.txt"/>
            <include name="changelog.html"/>
        </patternset>

        <!-- 4. Platform ZIPs -->
        <!-- Windows ZIP -->
        <zip destfile="${dist.dir}/web/CanReg5-Windows.zip">
            <zipfileset dir=".">
                <patternset refid="base.files"/>
            </zipfileset>
            <zipfileset dir="conf/tables/r/r-packages" includes="*.tar.gz" prefix="conf/tables/r/r-packages"/>
            <zipfileset dir="doc/CanReg5-Instructions" includes="CanReg5-Instructions.pdf" prefix="doc" erroronmissingdir="false"/>
            <zipfileset dir="${dist.dir}" includes="CanReg.jar"/>
            <zipfileset dir="${dist.dir}/lib" prefix="lib"/>
            <zipfileset dir="jdeploy/installers" includes="*win*.exe"/>
        </zip>

        <!-- macOS App Bundle (only if building on Mac) -->
        <condition property="isMac">
            <os family="mac"/>
        </condition>
        <antcall target="-build-mac-app"/>

        <!-- Linux ZIP -->
        <zip destfile="${dist.dir}/web/CanReg5-Linux.zip">
            <zipfileset dir=".">
                <patternset refid="base.files"/>
            </zipfileset>
            <zipfileset dir="conf/tables/r/r-packages" includes="*.tar.gz" prefix="conf/tables/r/r-packages"/>
            <zipfileset dir="doc/CanReg5-Instructions" includes="CanReg5-Instructions.pdf" prefix="doc" erroronmissingdir="false"/>
            <zipfileset dir="${dist.dir}" includes="CanReg.jar"/>
            <zipfileset dir="${dist.dir}/lib" prefix="lib"/>
            <zipfileset dir="jdeploy/installers" includes="*linux*"/>
        </zip>

        <!-- 5. Copy metadata files to web directory -->
        <copy todir="${dist.dir}/web">
            <fileset dir="." includes="changelog.txt,changelog.html,version.txt"/>
            <fileset file="doc/CanReg5-Instructions/CanReg5-Instructions.pdf" erroronmissingdir="false"/>
        </copy>
    </target>

    <target name="-build-mac-app" if="isMac">
        <echo>Creating macOS App Bundle...</echo>
        <exec executable="/bin/bash" failonerror="false">
            <arg value="helpers/create-mac-app.sh"/>
        </exec>
        <zip destfile="${dist.dir}/web/CanReg5-macOS.zip">
            <zipfileset dir="." includes="CanReg5.app/**" excludes="CanReg5.app/Contents/MacOS/CanReg5"/>
            <zipfileset dir="." includes="CanReg5.app/Contents/MacOS/CanReg5" filemode="755"/>
        </zip>
    </target>

    <target name="-post-jar">
        <exec executable="ruby" osfamily="unix" failonerror="false">
            <arg value="update_version_number.rb"/>
        </exec>
        <exec executable="cmd" osfamily="windows" failonerror="false">
            <arg value="/c"/>
            <arg value="ruby"/>
            <arg value="update_version_number.rb"/>
        </exec>
        
        <!-- Package ZIPs (which depends on jDeploy) -->
        <antcall target="-package-zips"/>
        
        <echo>Building setup...</echo>
        <exec executable="cmd" osfamily="windows" failonerror="false">
            <arg value="/c"/>
            <arg value="inno-build.bat"/>
        </exec>
    </target>
</project>
